#include "main.h"

/**
 * _abs - A function that prints the absolute value
 * @n: the int to check
 * Return: an absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
